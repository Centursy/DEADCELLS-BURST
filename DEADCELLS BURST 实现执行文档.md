# DEADCELLS BURST 实现执行文档

本文档用于把 `DEADCELLS BURST.md` 转换为 Koishi 插件实现。`koishi-plugin-impart-pro` 只作为 Koishi 插件组织方式、数据库扩展和指令注册的参考，不作为游戏规则来源。

## 一、实现目标

第一阶段完成纯文字可玩的 QQ 群聊版本：

- `deadcells`：创建角色或查询角色状态
- `修炼`：按地图深度探索并获得细胞
- `boss细胞`：消耗当前库存细胞提升细胞等级
- `对战 @用户`：双方进行回合制自动战斗
- 战斗结束后结算细胞和装备掉落
- 装备替换使用 `y` 确认，不建立背包
- QQ 平台使用转发聊天记录发送战斗过程
- 后续再增加 Puppeteer 状态卡和战斗结果图

## 二、已确定的游戏规则

### 角色

- 初始最大生命值：50
- 初始基础攻击力：10
- 初始武器：生锈的刀
- 初始盾牌：无
- 生锈的刀提供攻击力 `+5`，因此初始显示攻击力为 15
- 战斗中的 HP 是临时战斗属性，不写回玩家的持久生命值
- 诅咒之刃装备后，角色最大生命值直接变为 1

### 细胞等级

细胞等级为 0 到 5。升级消耗玩家当前库存中的细胞，消耗后从库存扣除：

| 目标等级 | 消耗细胞 | 探索倍率 | 额外最大 HP | 额外攻击 | 额外暴击率 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 1 | 1000 | 2 | 10 | 5 | 10% |
| 2 | 2000 | 3 | 10 | 5 | 10% |
| 3 | 3000 | 4 | 10 | 5 | 10% |
| 4 | 4000 | 5 | 10 | 5 | 10% |
| 5 | 5000 | 5 | 10 | 5 | 10% |

升级时使用当前等级对应的消耗，不是累计消耗。5 细胞等级为最高等级。

### 修炼

探索从“被囚者的牢房”开始：

1. 第一张地图必定到达。
2. 每次尝试进入下一张地图时，按该地图的到达概率抽取结果。
3. 探索在某张地图失败后结束，奖励本次已经到达的所有地图的细胞总和。
4. 总和再乘以当前细胞等级倍率。
5. 到达“王座之间”后，再额外抽取 60% 概率挑战成功。
6. Boss 挑战成功额外获得 100 细胞，失败仍保留前面已经到达地图的奖励。

例如：1 细胞等级到达第二张地图，奖励为 `(10 + 15) × 2 = 50`。

### 对战

- 对战双方必须已经创建角色。
- 随机决定先手。
- 双方轮流攻击，直到一方 HP 降为 0。
- 暴击伤害为普通伤害的 2 倍。
- 胜者获得战败者当前库存细胞的 20%，向上取整。
- 这部分细胞从战败者库存扣除，不能凭空生成。
- 胜者有 42% 概率获得一件装备。
- 装备允许重复掉落。
- 没有背包，拒绝替换后装备直接丢弃；想再次获得只能继续对战掉落。

### 装备触发

- 胡桃夹子每次攻击有 30% 概率使对手眩晕一回合。
- 刺客匕首每次攻击有 30% 概率使用“背刺”，该次攻击必定暴击。
- 武士刀每次攻击有 30% 概率使用“穿刺”，该次攻击必定暴击。
- 大剑使用者的第二次和第三次攻击必定暴击。
- 诅咒之刃攻击必定暴击，并将最大生命值设为 1。
- 狂暴之刃在当前 HP 低于最大 HP 的 50% 时必定暴击。
- 对被冻结的目标进行攻击时必定暴击。
- 所有盾牌基础格挡率为 20%。
- 击晕盾、寒冰盾的控制效果持续一回合。
- 壁垒盾的无敌只保护下一次受到攻击的机会。
- 尖刺盾反弹本次攻击产生的伤害，默认按格挡前的伤害计算。
- 贪婪盾格挡成功时立即偷取对手当前细胞的 10%。

## 三、建议的数据结构

只保存装备 ID，不把装备属性写进玩家记录。装备属性由静态装备定义提供，这样以后调整平衡不需要迁移玩家数据。

### `deadcells_players`

```ts
interface DeadcellsPlayer {
  userId: string
  username: string
  cells: number
  bossCellLevel: number
  weaponId: string
  shieldId: string | null
  battleCount: number
  winCount: number
  lastBattleAt: number
}
```

主键使用 `userId`，角色数据默认跨群共享。`bossCount` 不实现，因为它已经从当前设计中废弃。

### 静态装备定义

装备定义单独放在 `src/data/equipment.ts`：

```ts
interface EquipmentDefinition {
  id: string
  name: string
  type: 'weapon' | 'shield'
  description: string
  attackBonus?: number
  blockRate?: number
  effect: string
}
```

战斗引擎通过装备 ID 查找定义，不建立背包表，也不保存掉落数量。

## 四、代码结构

```text
src/
  index.ts                 插件入口、数据库表、服务注入
  config.ts                Koishi 配置 Schema
  types.ts                 数据库和战斗类型
  data/
    maps.ts                地图、概率、奖励、评价
    equipment.ts           15 件装备的静态定义
  core/
    progression.ts         初始角色、升级、属性计算
    exploration.ts         修炼探索算法
    battle.ts              纯战斗模拟器
    rewards.ts             细胞和装备掉落结算
  commands/
    character.ts           deadcells
    explore.ts             修炼
    upgrade.ts             boss细胞
    duel.ts                对战
  output/
    text.ts                状态和结果文字格式化
    qq-forward.ts          QQ 转发消息节点构造
    image.ts               后续 Puppeteer 图片渲染
  utils/
    user.ts                @用户解析、昵称获取
    prompt.ts              y/n 确认和超时处理
```

## 五、战斗引擎实现步骤

### 1. 生成战斗快照

从玩家持久数据计算本局属性：

- 最大 HP = 50 + 细胞等级额外 HP
- 攻击力 = 10 + 细胞等级额外攻击 + 武器攻击加成
- 暴击率 = 细胞等级暴击率加成
- 诅咒之刃覆盖最大 HP 为 1
- 每位玩家复制一份临时战斗状态，不直接修改数据库

临时状态包含当前 HP、是否眩晕、是否冰冻、是否无敌、攻击次数、武器叠层和本局细胞数。

### 2. 处理回合

每回合执行：

1. 检查攻击者是否眩晕或冰冻，是则跳过并清除状态。
2. 计算本次攻击的基础攻击力。
3. 根据装备和当前状态判断是否触发技能或必定暴击。
4. 检查防御方盾牌是否格挡。
5. 处理伤害、暴击、反伤、控制、无敌和细胞偷取。
6. 记录一条结构化战斗事件。
7. 检查是否有人 HP 归零。

战斗引擎设置最大回合数，防止未来新增无敌或控制效果后出现死循环。超过上限时按剩余 HP 和基础战斗力判定胜负。

### 3. 保存结果

战斗模拟完成后，统一更新双方的对战次数、胜场次数、细胞库存和装备状态。战斗开始到结算期间使用用户锁，避免同一玩家同时参与两场对战造成重复扣除或奖励。

## 六、指令交互实现步骤

### `deadcells`

- 查询不到数据时创建初始角色并返回角色状态。
- 查询到数据时返回当前状态。
- 使用 Koishi 的 command description 让 `help deadcells` 显示玩法说明。

### `修炼`

- 查询角色，不存在时提示先使用 `deadcells`。
- 执行探索算法。
- 一次性更新细胞库存。
- 返回到达地图、每张地图奖励、总奖励和评价。

### `boss细胞`

- 查询当前等级、当前细胞和升级所需细胞。
- 细胞不足或已经达到 5 级时直接返回。
- 细胞足够时使用 `session.prompt()` 等待当前用户回复。
- 只有回复 `y` 或 `Y` 才扣除细胞并升级，其他回复取消。
- 设置确认超时，超时不修改数据。

### `对战 @用户`

- 解析 QQ 的 at 元素并禁止自己攻击自己。
- 检查双方角色是否存在。
- 获取双方战斗快照并运行战斗引擎。
- 用 QQ 转发消息发送完整战斗日志。
- 普通消息发送战斗结算和细胞变化。
- 42% 掉落装备时，发送装备信息并等待胜者回复 `y`。
- 确认后替换对应装备；取消或超时则丢弃装备。

## 七、QQ 转发消息实现

战斗引擎输出结构化事件，不直接拼接 QQ 消息。`qq-forward.ts` 将事件转换为转发节点：

- 战斗开始
- 双方属性
- 每个回合的行动和伤害
- 格挡、暴击、控制、反伤等特殊事件
- 当前双方 HP
- 战斗结束

QQ 适配器支持转发节点时发送转发消息；其他适配器使用纯文字合并输出作为降级方案。装备替换确认和最终奖励使用普通消息，避免用户无法在转发消息中回复。

## 八、Puppeteer 后续实现

第一阶段不让图片渲染参与核心逻辑。后续增加独立渲染器：

1. `renderCharacterCard(player)`：角色状态卡。
2. `renderBattleResult(result)`：战斗结果卡。
3. `renderEquipment(equipment)`：装备掉落卡。

插件将 Puppeteer 设为可选服务。开启时返回 `h.image(buffer, 'image/png')`，未开启时沿用文字输出。静态背景和装备图片放在 `assets/`，动态数据通过 HTML 模板填充，并对用户昵称进行 HTML 转义。

## 九、配置项

配置 Schema 默认提供以下可调整项目：

- 指令名称
- 地图到达概率和基础细胞奖励
- 各细胞等级升级消耗和探索倍率
- 对战细胞转移比例，默认 20%
- 装备掉落概率，默认 42%
- 胡桃夹子眩晕概率，默认 30%
- 刺客匕首和武士刀技能概率，默认 30%
- 对战最大回合数
- 是否启用 QQ 转发战斗日志
- 是否启用 Puppeteer 图片输出

## 十、验证计划

### 单元测试

- 地图概率和累计奖励计算
- 细胞等级升级扣除当前库存
- 细胞不足、满级升级
- 先手随机和回合交替
- 暴击伤害翻倍
- 三十概率技能触发
- 三次大剑暴击规则
- 诅咒之刃最大 HP 为 1
- 六类盾牌效果
- 冰冻、眩晕和无敌持续时间
- 战败者细胞扣除和胜者向上取整
- 装备重复掉落和拒绝替换

### 手工 QQ 测试

- 首次发送 `deadcells`
- 两个用户创建角色后执行 `对战 @用户`
- 检查长战斗日志是否能作为转发消息发送
- 测试装备替换的 `y`、其他文字和超时
- 测试多人同时发起对战
- 测试没有 Puppeteer 或不支持转发的适配器降级输出

## 十一、目前仍需确认的边界

以下内容不影响整体可行性，但编码前最好确定：

1. 5 细胞等级的探索倍率确认为 `×5`。
2. 胡桃夹子眩晕在攻击命中后判定，概率为 30%。
3. 尖刺盾反弹本次攻击的最终伤害，包含暴击倍率。
4. 大剑第二、第三次必定暴击按本局发动攻击次数计算。
5. 装备掉落暂时在全部装备中等概率抽取，允许重复。
6. 角色数据按 QQ userId 跨群共享。
7. 修炼冷却时间为 30 秒，对战冷却时间为 60 秒。

以上规则已确认，后续实现不再作为待定项。

## 十二、开发顺序

1. 创建插件包和 TypeScript 构建配置。
2. 创建数据库模型、类型定义和静态地图/装备数据。
3. 完成角色创建、状态查询和细胞等级升级。
4. 完成可测试的修炼探索核心。
5. 完成不依赖 Koishi 的纯战斗模拟器。
6. 接入对战指令、QQ at 解析和数据库结算。
7. 接入 QQ 转发战斗日志和装备替换确认。
8. 编写单元测试并修正边界规则。
9. 增加 Koishi 配置页面所需的 Schema 描述。
10. 最后增加 Puppeteer 图片渲染，不改变核心玩法和数据结构。
