import type { BattleResult, DeadcellsPlayer, GameConfig, Random } from '../types';
export declare function simulateBattle(first: DeadcellsPlayer, second: DeadcellsPlayer, config: GameConfig, random?: Random): BattleResult;
